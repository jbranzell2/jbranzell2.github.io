node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Local Claude Code dev-server config (machine-specific, not part of the app).
# .claude/skills/ IS committed — project skills should travel with the repo.
/.claude/launch.json
